[package]
name = "mcp-logistics"
version = "1.1.1"
edition = "2024"
rust-version = "1.94.1"
authors = ["James Karanja <james.karanja@zavora.ai>"]
description = "Enterprise Logistics MCP Server — shipments, carriers, tracking, route optimization, and warehouse management with ShipEngine, Shippo, Sendy (Africa), and Google Maps"
license = "Apache-2.0"
repository = "https://github.com/zavora-ai/mcp-logistics"
homepage = "https://enterprise.adk-rust.com"
keywords = ["mcp", "logistics", "shipping", "tracking", "supply-chain"]
categories = ["development-tools", "api-bindings"]
readme = "README.md"

[features]
default = ["shipengine"]
shipengine = []
shippo = []
routes = []
all-backends = ["shipengine", "shippo", "routes"]

[dependencies]
adk-mcp-sdk = { version = "0.2.0", git = "https://github.com/zavora-ai/adk-mcp-sdk.git", rev = "4dac4301441a8a51a042fd66cc1798f0ec3e3f55" }
rmcp = { version = "3.1.2", features = ["server", "transport-io", "macros"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false }
anyhow = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
async-trait = "0.1"
schemars = "1"
chrono = { version = "0.4", features = ["serde"] }
dotenvy = "0.15"

[workspace]
